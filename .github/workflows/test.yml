name: Build APK

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 👇 关键：用 AGP 兼容的 JDK 版本，不乱升级
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      # 👇 直接用项目自带 gradlew，不自己生成 wrapper
      - name: Grant execute permission
        run: chmod +x gradlew

      # 👇 直接编译，GitHub 自带 Android SDK，不用你装
      - name: Build release APK
        run: ./gradlew assembleRelease

      # 👇 上传到 Action 页面供下载
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SoftApHelper-APK
          path: "**/build/outputs/apk/release/*.apk"
